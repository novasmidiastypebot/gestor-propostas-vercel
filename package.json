{
  "name": "efi-proxy",
  "version": "1.0.0",
  "description": "Servidor proxy para integração com API Efí (suporte a mTLS)",
  "main": "api/efi.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "sdk-node-apis-efi": "^2.3.2"
  },
  "engines": {
    "node": "22.x"
  }
}
