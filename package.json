{
  "name": "efi-proxy",
  "version": "1.0.0",
  "description": "Servidor proxy para integração com API Efí (suporte a mTLS)",
  "main": "api/efi.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "gn-api-sdk-node": "^2.2.1"
  },
  "engines": {
    "node": "22.x"
  }
}
